services:
  mongodb:
    image: mongo:7
    container_name: hackaton10-mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: root
      MONGO_INITDB_ROOT_PASSWORD: root123
      MONGO_INITDB_DATABASE: hackaton10
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
