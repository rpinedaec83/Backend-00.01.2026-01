-- Coloca los typos de typescript,
yarn add -D typescript ts-node

-- trae los tipos de node y de express
yarn add -D @types/node @types/express

-- crea el archivo de configuracion para typescript
npx tsc --init
